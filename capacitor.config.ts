import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sakambari.market',
  appName: 'Sri Sakambari',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
